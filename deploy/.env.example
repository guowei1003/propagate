# Database
POSTGRES_PASSWORD=postgres

# LLM Provider (demo | openai | ollama)
LLM_PROVIDER=demo
LLM_API_BASE=
LLM_API_KEY=
LLM_MODEL=gpt-4o-mini

# Worker
WORKER_CONCURRENCY=3

# Network
DOCKER_NETWORK=propagate-net
RUNNER_IMAGE=propagate-runner:latest
